function calculate(s: string): number {
  while(s) {

  }
};

function operation(num1, num2, sign) {
  switch(sign) {
    case '+': return num1 + num2;
    case '-': return num1 - num2;
    case '*': return num1 * num2;
    case '/': return num1 / num2;
  }
}
